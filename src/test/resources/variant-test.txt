variant Student { int id; string name; };

fn main() {
    Student student = Student::id(33);
    # Student student = Student::name("jarcin");

    match(student) {
        Student::id(x) {
            print((string)x);
        }
        Student::name(name) {
            print(name);
        }
    }
}