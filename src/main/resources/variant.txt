variant Student { int id; string name; };

fn main() {
    # Student debil = Student::id(5);
    Student debil = Student::name("debil");
    match(debil) {
        Student::id(x) {
            print((string)x);
        }
        Student::name(name) {
            print(name);
        }
    }
}