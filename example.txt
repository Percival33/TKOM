variant Student { int id; string name; };

fn inspect(Student s): int {
    match(s) {
        Student::id(x) {
            return x;
        }
    }
    print("plz not print");
}

fn main() {
    Student s = Student::id(1);
    int a = inspect(s);
    print((string)a);
}